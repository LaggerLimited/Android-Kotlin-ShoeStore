package com.laggerlimited.shoestore.login

import androidx.lifecycle.ViewModel

/**
 * The LoginViewModel is empty for now. This can be used to retain information entered by the user
 * on the LoginFragment screen over various lifecycle changes.
 */
class LoginViewModel : ViewModel() {
    init{

    }

    override fun onCleared() {
        super.onCleared()

    }
}